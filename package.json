{
  "name": "nexstock",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/api",
    "apps/web"
  ],
  "scripts": {
    "dev": "concurrently \"npm run start:dev -w @nexstock/apps/api\" \"npm run dev -w @nexstock/apps/web\"",
    "build": "npm run build -w @nexstock/apps/api && npm run build -w @nexstock/apps/web",
    "api": "npm run start:dev -w @nexstock/apps/api",
    "web": "npm run dev -w @nexstock/apps/web",
    "postinstall": "cd apps/api && npx prisma generate",
    "migrate": "npm run migrate:deploy",
    "migrate:deploy": "cd apps/api && npx prisma db push",
    "deploy": "npm install && npm run migrate && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
